/* Palindromic Ranges.cpp
 *
 *
 *
 */

#include <iostream>
#include <fstream>
#include <string>

int main(int argc, char * argv[]) {
	ifstream ifs;

	ifs.open(argv[1]);

	while(ifs.peek() != EOF) {
		
	}
}